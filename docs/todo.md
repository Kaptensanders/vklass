# Todo

* remove the /workspaces/vklass/test/sandbox/cookie.txt cookie dump from HA integration before release


* if login fails with exception, auth sensor is left in "inprogress"

* seems to be no recovery for the auth sensor, even a ha restart keeps "inprogress"
